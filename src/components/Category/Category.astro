---
import { getLocaleFromUrl } from "@js/localeUtils";
import { humanize, slugify } from "@js/textUtils";

import { getLocalizedRoute } from "@/js/translationUtils";

interface Props {
	category: string;
}

const { category } = Astro.props as Props;

const currLocale = getLocaleFromUrl(Astro.url);
---

<a
	class="text-primary-600 hover:text-primary-700 dark:text-primary-400 dark:hover:text-primary-300 inline-block text-sm font-medium tracking-tight transition-colors md:text-base"
	href={getLocalizedRoute(currLocale, `/categories/${slugify(category)}`)}
>
	{humanize(category)}
</a>
